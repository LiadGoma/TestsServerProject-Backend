const respondentController= require("../controllers/resondentController");
const express = require('express');
const router = express.Router();


router.post('/', async (req, res) => {
   
});

module.exports = router;